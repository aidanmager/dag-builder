USE DATABASE {{DATABASE}};

---

COPY INTO {{DATABASE}}.{{INGEST_SCHEMA}}.{{TABLE_NAME}} FROM (
SELECT
	$1::VARIANT AS RAW_DATA,
	METADATA$FILENAME AS __FILENAME,
	METADATA$FILE_LAST_MODIFIED AS __FILE_LAST_MODIFIED,
	'{{ data_interval_start.strftime("%Y%m%d") }}'::VARCHAR AS DAG_RUN_KEY
FROM
	@{{DATABASE}}.{{INGEST_SCHEMA}}.{{STAGE_NAME}}/{{TABLE_NAME}}/
);